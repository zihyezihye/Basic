#백준 10807번 응용

#리스트를 활용하고, max함수와 min함수없이 풀어보기

a=list(map(int,input().split()))
max=a[0]
min=a[0]

#최댓값 구하기
for i in a:
    if i >= max :
        max = i
print(max)

#최솟값 구하기
for j in a:
    if j <= min :
        min = j
print(min)

#나머지 중간값 출력
mid=0
for k in a :
    if k != max and k != min: 
        mid=k
print(mid)
